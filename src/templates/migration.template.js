// Migration $migrationName

/*
 * This is your migration script, you should export an array of tuples specifying each operation's up and down function.
 * You may add additional non-structural changes to your database such as data processing and assigning defaults to
 * non-null fields before their constraints are applied.
 * Do not however perform structure manipulations or modify the generated raw queries.
 *
 */

module.exports = [
	$operations
]
